# Verification Instruction - S2FE1

## Verification Target
S2FE1 — 메인 홈페이지 (`index.html`) 구현

## Verification Agent
code-reviewer-기본

## Verification Checklist

### 1. 기능 검증
- [x] 히어로(Hero) 섹션이 정상 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 에피소드 미리보기 섹션이 최신 에피소드 데이터를 표시하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 명언(Quote) 섹션이 정상 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 커뮤니티 섹션(링크/미리보기)이 정상 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 모든 CTA(Call To Action) 버튼의 링크가 올바른 페이지로 연결되는지 확인 (소급 도입 — 이미 확인됨)

### 2. 코드 품질
- [x] `common.js`의 `renderHeader()` 및 `renderFooter()`가 호출되어 헤더·푸터가 정상 삽입되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 시맨틱 HTML 태그(`<main>`, `<section>`, `<article>` 등)가 적절히 사용되었는지 확인 (소급 도입 — 이미 확인됨)
- [x] 인라인 스타일 없이 CSS 클래스를 통해 스타일이 적용되는지 확인 (소급 도입 — 이미 확인됨)

### 3. 통합 검증
- [x] 모바일(375px)에서 히어로 이미지 및 텍스트가 깨지지 않고 반응형으로 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 에피소드 미리보기 데이터가 `episode-content.js`의 EPISODES 배열과 연동되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 페이지 로드 시 콘솔 오류(console error)가 없는지 확인 (소급 도입 — 이미 확인됨)

## Acceptance Criteria
- [x] 홈페이지의 모든 4개 섹션(히어로, 에피소드 미리보기, 명언, 커뮤니티)이 정상 렌더링됨 (소급 도입 — 통과)
- [x] `common.js` 헤더·푸터가 정상 동작함 (소급 도입 — 통과)
- [x] 모바일 반응형이 정상 동작함 (소급 도입 — 통과)

## Remarks
- 소급 도입. 이미 검증 완료.
- "공대생 할아버지, 미대생 할머니" 40년 동행 부부 이야기를 소개하는 랜딩 페이지.

# Verification Instruction - S2FE3

## Verification Target
S2FE3 — 에피소드 상세 페이지 (`episode.html`) 구현

## Verification Agent
code-reviewer-기본

## Verification Checklist

### 1. 기능 검증
- [x] URL 파라미터 `?ep=N` 파싱이 정상적으로 수행되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 파싱된 에피소드 번호에 해당하는 본문 콘텐츠가 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 삽화(illustration)가 에피소드 내용과 연동되어 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 이전 에피소드 / 다음 에피소드 네비게이션 버튼이 정상 동작하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 존재하지 않는 에피소드 번호(`?ep=999` 등) 접근 시 적절한 오류 처리(리다이렉트 또는 안내 메시지)가 되는지 확인 (소급 도입 — 이미 확인됨)

### 2. 코드 품질
- [x] `URLSearchParams`를 사용하여 파라미터를 파싱하는지 확인 (소급 도입 — 이미 확인됨)
- [x] `episode-content.js`의 EPISODES 배열에서 해당 에피소드 객체를 찾아 동적 렌더링하는지 확인 (소급 도입 — 이미 확인됨)
- [x] `common.js`의 헤더·푸터가 정상 삽입되는지 확인 (소급 도입 — 이미 확인됨)

### 3. 통합 검증
- [x] 에피소드 목록(`episodes.html`) → 상세 페이지 → 이전/다음 네비게이션 플로우가 전체적으로 동작하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 첫 번째 에피소드에서 "이전" 버튼이 비활성화 또는 숨김 처리되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 마지막 에피소드에서 "다음" 버튼이 비활성화 또는 숨김 처리되는지 확인 (소급 도입 — 이미 확인됨)

## Acceptance Criteria
- [x] `?ep=N` 파라미터로 해당 에피소드 본문이 정확히 렌더링됨 (소급 도입 — 통과)
- [x] 이전/다음 네비게이션이 정상 동작함 (소급 도입 — 통과)
- [x] 존재하지 않는 에피소드 접근 시 오류 처리가 동작함 (소급 도입 — 통과)

## Remarks
- 소급 도입. 이미 검증 완료.
- 정적 사이트에서 라우팅 없이 URL 파라미터만으로 동적 콘텐츠를 렌더링하는 패턴을 사용함.

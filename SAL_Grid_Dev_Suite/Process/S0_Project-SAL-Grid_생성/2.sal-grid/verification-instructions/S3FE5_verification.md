# Verification Instruction - S3FE5

## Verification Target
S3FE5 — 플랫폼 페이지 (`platform.html` 또는 `why-us.html`) 구현

## Verification Agent
code-reviewer-기본

## Verification Checklist

### 1. 기능 검증
- [x] 파트너 섹션이 정상 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] NKCL 제안서 링크가 존재하고 올바른 URL로 연결되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 파트너 정보(로고, 이름, 설명)가 정상 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 페이지 내 모든 앵커 링크(#섹션)가 정상 동작하는지 확인 (소급 도입 — 이미 확인됨)

### 2. 코드 품질
- [x] NKCL 제안서 링크가 `<a href="...">` 태그로 마크업되어 있는지 확인 (소급 도입 — 이미 확인됨)
- [x] 외부 링크에 `target="_blank"` 및 `rel="noopener noreferrer"` 속성이 설정되어 있는지 확인 (소급 도입 — 이미 확인됨)
- [x] `common.js`의 헤더·푸터가 정상 삽입되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 파트너 섹션 레이아웃이 CSS 변수를 활용하여 일관된 디자인을 유지하는지 확인 (소급 도입 — 이미 확인됨)

### 3. 통합 검증
- [x] 네비게이션 드롭다운(있는 경우)에서 NKCL 제안서 링크가 노출되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 모바일(375px)에서 파트너 섹션이 적절히 반응형으로 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 페이지 로드 시 콘솔 오류가 없는지 확인 (소급 도입 — 이미 확인됨)

## Acceptance Criteria
- [x] 파트너 섹션이 정상 렌더링됨 (소급 도입 — 통과)
- [x] NKCL 제안서 링크가 정상 동작함 (소급 도입 — 통과)
- [x] 외부 링크 보안 속성이 설정됨 (소급 도입 — 통과)

## Remarks
- 소급 도입. 이미 검증 완료.
- NKCL(도서관 또는 파트너 기관) 제안서 링크는 프로젝트의 출판·배포 협력 맥락에서 중요한 페이지임.

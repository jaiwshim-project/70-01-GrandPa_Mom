# Verification Instruction - S2BI1

## Verification Target
S2BI1 — 공통 헤더·푸터 모듈 (`common.js`) 구현

## Verification Agent
code-reviewer-기본

## Verification Checklist

### 1. 기능 검증
- [x] `renderHeader()` 함수가 모든 페이지에서 헤더를 정상 삽입하는지 확인 (소급 도입 — 이미 확인됨)
- [x] `renderFooter()` 함수가 모든 페이지에서 푸터를 정상 삽입하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 헤더 탑바(예: 부제목 또는 태그라인)가 정상 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 모바일 햄버거 메뉴 토글 버튼이 동작하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 메뉴가 열린 상태에서 다른 영역 클릭 시 메뉴가 닫히는지 확인 (소급 도입 — 이미 확인됨)

### 2. 코드 품질
- [x] 헤더·푸터 HTML이 `common.js`에 중앙화되어 중복 없이 관리되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 현재 페이지의 네비게이션 링크가 활성(active) 상태로 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 모든 네비게이션 링크가 올바른 경로를 가리키는지 확인 (소급 도입 — 이미 확인됨)

### 3. 통합 검증
- [x] `index.html`, `episodes.html`, `gallery.html`, `community.html` 등 전 페이지에서 헤더·푸터가 동일하게 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 모바일(375px)에서 햄버거 메뉴가 나타나고 데스크톱에서 일반 네비게이션이 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] `common.js` 변경 시 전 페이지에 즉시 반영되는 구조인지 확인 (소급 도입 — 이미 확인됨)

## Acceptance Criteria
- [x] 모든 페이지에서 `renderHeader()`와 `renderFooter()`가 정상 동작함 (소급 도입 — 통과)
- [x] 모바일 메뉴 토글이 정상 동작함 (소급 도입 — 통과)
- [x] 헤더 탑바가 모든 페이지에서 렌더링됨 (소급 도입 — 통과)

## Remarks
- 소급 도입. 이미 검증 완료.
- 정적 사이트에서 헤더·푸터 중복 제거를 위해 JS 기반 동적 삽입 패턴을 사용함.

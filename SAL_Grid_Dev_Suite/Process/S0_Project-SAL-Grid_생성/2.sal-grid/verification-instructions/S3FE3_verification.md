# Verification Instruction - S3FE3

## Verification Target
S3FE3 — 책 프로젝트 페이지 (`book-project.html`) 구현

## Verification Agent
code-reviewer-기본

## Verification Checklist

### 1. 기능 검증
- [x] 카운트다운 타이머가 2026-08-01 목표일을 기준으로 정상 작동하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 카운트다운이 일(D), 시(H), 분(M), 초(S) 단위로 표시되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 타임라인이 3월~8월 단계를 올바르게 표시하는지 확인 (소급 도입 — 이미 확인됨)
- [x] 진행률 바(progress bar)가 현재 날짜 기준으로 올바른 퍼센트를 표시하는지 확인 (소급 도입 — 이미 확인됨)

### 2. 코드 품질
- [x] 카운트다운이 `setInterval`로 1초마다 갱신되는지 확인 (소급 도입 — 이미 확인됨)
- [x] CSS 변수(`--navy`, `--terra`, `--gold` 등)가 하드코딩 없이 사용되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 타임라인 데이터가 배열로 관리되어 동적 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] `common.js`의 헤더·푸터가 정상 삽입되는지 확인 (소급 도입 — 이미 확인됨)

### 3. 통합 검증
- [x] 목표일(2026-08-01) 이후에도 카운트다운이 '출간 완료' 등의 메시지로 정상 처리되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 진행률 바가 디자인 시스템 색상 변수를 사용하여 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 모바일(375px)에서 타임라인과 카운트다운이 적절히 반응형으로 표시되는지 확인 (소급 도입 — 이미 확인됨)

## Acceptance Criteria
- [x] 카운트다운이 2026-08-01 기준으로 정상 작동함 (소급 도입 — 통과)
- [x] 타임라인(3월~8월)이 정상 렌더링됨 (소급 도입 — 통과)
- [x] 진행률 바가 CSS 변수를 사용하여 렌더링됨 (소급 도입 — 통과)

## Remarks
- 소급 도입. 이미 검증 완료.
- 책 출간 목표일 2026-08-01은 부부 40주년 기념일과 연계된 상징적 날짜임.

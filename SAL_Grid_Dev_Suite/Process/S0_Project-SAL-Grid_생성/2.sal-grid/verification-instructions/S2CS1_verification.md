# Verification Instruction - S2CS1

## Verification Target
S2CS1 — 에피소드 콘텐츠 데이터 (`episode-content.js`) 작성

## Verification Agent
qa-specialist

## Verification Checklist

### 1. 기능 검증
- [x] `EPISODES` 배열이 `export` 또는 전역 변수로 정상 접근 가능한지 확인 (소급 도입 — 이미 확인됨)
- [x] 각 에피소드 객체에 필수 필드(id, title, part, content 등)가 모두 포함되는지 확인 (소급 도입 — 이미 확인됨)
- [x] 파트별 분류(Part 1, Part 2, Part 3 등)가 각 에피소드에 정확히 할당되어 있는지 확인 (소급 도입 — 이미 확인됨)
- [x] 에피소드 ID가 1부터 순차적으로 중복 없이 부여되어 있는지 확인 (소급 도입 — 이미 확인됨)

### 2. 코드 품질
- [x] EP.2 본문에서 '그럴 리가'가 올바른 맞춤법으로 작성되어 있는지 확인('그럴리가' 오타 없음) (소급 도입 — 이미 확인됨)
- [x] 전체 에피소드 텍스트에 명백한 맞춤법 오류가 없는지 확인 (소급 도입 — 이미 확인됨)
- [x] 에피소드 콘텐츠가 적절한 분량(너무 짧거나 길지 않음)인지 확인 (소급 도입 — 이미 확인됨)
- [x] JS 배열 문법(쉼표, 괄호 등)이 올바른지 확인 (소급 도입 — 이미 확인됨)

### 3. 통합 검증
- [x] `episodes.html`에서 EPISODES 배열의 모든 에피소드가 렌더링되는지 확인 (소급 도입 — 이미 확인됨)
- [x] `episode.html?ep=N`에서 해당 에피소드 객체가 정확히 조회되는지 확인 (소급 도입 — 이미 확인됨)
- [x] `illustrations.js`의 에피소드 ID 참조가 `episode-content.js`의 ID와 일치하는지 확인 (소급 도입 — 이미 확인됨)

## Acceptance Criteria
- [x] EPISODES 배열의 데이터 무결성이 검증됨 (소급 도입 — 통과)
- [x] 파트별 분류가 정확함 (소급 도입 — 통과)
- [x] EP.2 '그럴 리가' 맞춤법이 올바르게 수정되어 있음 (소급 도입 — 통과)

## Remarks
- 소급 도입. 이미 검증 완료.
- '그럴 리가'는 의존 명사 '리'와 보조사 '가'가 결합된 형태로 띄어쓰기가 필요함.
